export type TRoutesUrl = {
  readonly [name: string]: string;
};

export type TCardBig = {
  id: number;
  text: string;
};

export type TArrImage = {
  id: number;
  image: string;
  alt: string;
};
